services:
  app:
    build: .
    container_name: system_b_registry
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: always

volumes:
  sqlite_data:
